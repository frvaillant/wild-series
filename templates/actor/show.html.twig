{% extends 'base.html.twig' %}

{% block title %}Actor{% endblock %}

{% block body %}
    {% include 'admin/_admin_nav.html.twig' %}
<div class="container">

    <h1>Actor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actor.id }}</td>
                <td>{{ actor.name }}</td>
                <td>
                    joue dans les séries
                    {% for program in actor.programs %}
                        <a class="btn btn-primary" href="{{ path('program_show', {'id': program.id}) }}">{{program.title}}</a>
                    {% endfor %}
                </td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('actor_index') }}">back to list</a>

    <a href="{{ path('actor_edit', {'id': actor.id}) }}">edit</a>

    {{ include('actor/_delete_form.html.twig') }}
</div>
{% endblock %}
